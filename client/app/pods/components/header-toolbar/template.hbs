{{#paper-toolbar as |toolbar|}}
  {{#toolbar.tools}}
    {{#paper-button raised=true onClick=(action "toggle" "sidebarOpenState.lockedOpen")}}
      {{t "header-toolbar.menu"}}
    {{/paper-button}}

    {{#unless session.isAuthenticated}}
      {{paper-link (t "header-toolbar.login") 'login' raised=true}}
    {{/unless}}
  {{/toolbar.tools}}
{{/paper-toolbar}}
