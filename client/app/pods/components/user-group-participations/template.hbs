{{user-group-participations/current-table
  groupParticipations=currentGroupParticipations
  isPending=user.groupParticipations.isPending
  isRejected=user.groupParticipations.isRejected
}}

{{user-group-participations/previous-table
  groupParticipations=previousGroupParticipations
  isPending=user.groupParticipations.isPending
  isRejected=user.groupParticipations.isRejected
}}
